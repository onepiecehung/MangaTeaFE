stages:
  - build
  - deploy

build_client-artifacts:
  stage: build
  image: trion/ng-cli
  before_script:
    - cd client
    - npm install
  script:
    - npm run build:prod
  artifacts:
    expire_in: 2 day
    paths:
      - client/dist/
  only:
    refs:
      - master
      - develop
