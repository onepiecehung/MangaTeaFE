import { SpliceStringPipe } from './splice-string.pipe';

describe('SpliceStringPipe', () => {
  it('create an instance', () => {
    const pipe = new SpliceStringPipe();
    expect(pipe).toBeTruthy();
  });
});
